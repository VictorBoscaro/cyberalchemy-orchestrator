# W0 — Immutable Baseline

- Layer question: what exactly exists before the first candidate mutation?
- Included work: read-only source hashes, Python/import/runtime test baseline,
  canonical profile ID/version/digest, and absence/presence of every SWU-001 target.
- Entry gate: exact Work Pack selected by user; no product mutation yet.
- Commands: hash `canonical.py`; parse `allowed-routes.json`; run the current
  targeted canonical tests and record the chosen runtime command/environment.
- Exit evidence: one immutable baseline bound to repository HEAD, target
  inventory, commands, results, and timestamps.
- Failure: baseline contradiction blocks W1; do not repair it under W0.

