# Stage 03 — Interrogation Refine Review

Capability: `interrogation`  
Mode: `refine-review`  
Verdict: FLAG

No additional user question was required: the confirmed seed and nine-agent dispatch fix the objective, alternatives, evidence corpus, write scope, proof ceiling, and review gates. The review asked the highest-discrimination questions against the definition.

| Question | Answer | Effect |
|---|---|---|
| Can all condition-like objects share one executable expression interface? | No; that permits RWO to absorb external owners. | kill universal evaluator |
| Should the seven forms use unrelated top-level schemas? | No; identity, provenance, compilation, defects, and validation are shared mechanics. | retain one minimal envelope |
| Does the common envelope include generic `conditions`, `policies`, or `limits` arrays? | No; each kind payload exposes only its closed admissible slots. | prevent disguised extension engine |
| Is `FormInstance` runtime execution state? | No; it is structural placement only. | keep WorkRun/Attempt/Round separate |
| Does a terminal or `approved` event imply domain success? | No; only selected structure or an opaque accepted route label is visible. | preserve domain owner |
| Does quorum mean agreement or quality? | No; if retained, it counts distinct eligible structural arrivals only. | preserve open quorum-owner gate |
| Can a late arrival rewrite an already materialized join invocation? | No. | require frozen manifest |
| Is BoundedRepeat a retry mechanism? | No; it allocates a new bounded round after an accepted decision and debit. | preserve recovery taxonomy |
| Can a compilation receipt authorize or promote an ontology? | No. | fixed `authority_effect: none` |
| Can the current ontology be migrated immediately? | No; its `0.1.0`/`0.2.0` identity divergence and owner gates block migration. | plan remediation first |

## Flags

1. Sequence’s n-ary surface versus binary realization needs an explicit owner decision.
2. Structural quorum ownership remains open.
3. The exact late-arrival policy vocabulary and owner remain open.
4. Journal truth, minimal authority reference, condition classification, reasoning entry, artifact admission, and exact-effect owners remain unresolved.
5. The current ontology identity diverges across semantic/current-state and machine-readable surfaces.

## Definition review verdict

Proceed to bounded multi-angle research and adversarial review. Candidate C remains viable, but no plan may authorize ontology migration or runtime implementation until the named owner gates and finite witness contracts are preserved.
