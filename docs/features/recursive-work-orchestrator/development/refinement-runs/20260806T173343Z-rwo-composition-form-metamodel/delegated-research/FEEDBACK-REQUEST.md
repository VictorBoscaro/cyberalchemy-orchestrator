# Bounded Synthesis Feedback Request

Loop: 1 of 1  
Target: Mac Lane, Saunders, prior synthesis action `spawn-0004`  
Reason: all reviewer actions completed, but the target verdicts were `FLAG`, `BLOCK`, and `BLOCK_REPAIRABLE`.

Required revision:

1. Kill generic `ConditionBinding`, condition subtypes, and generic `evaluated-by`; reuse `EventSelector`, `ReleasePolicy`, existing lifecycle policies, exact route behavior, definition limits, and typed external owner references.
2. Replace the current `FormInstance` with an optional compile-time `FormOccurrence` only if source occurrence identity is necessary; never duplicate `WorkNode`, `WorkRun`, `Attempt`, or round identity.
3. Treat an authored form definition as a concrete, immutable instance of the existing `CompositionForm` taxonomy, enclosed by `CompositeWorkDefinition`; avoid an ornamental parallel type.
4. Close operand roles per form and remove the universal target union.
5. Repair existing `compiles-to` into a disjoint `CompilationSuccess`/`CompilationRejection` lineage from source form definition to output `WorkGraph`; `ExplicitComposition` remains source syntax.
6. Restrict defects to compiler-owned structural defects and separate runtime/journal/external-owner results.
7. Use typed owner references; references never perform acceptance or grant authority.
8. Freeze sidecar cancellation direction and the repeat/redelivery/retry/replay/effect non-collapse rules.
9. Reclassify witness material honestly: the scenario list is a planned fixture contract, not finite/executed non-vacuity evidence. Split ambiguous defects and mutation boundaries.
10. Preserve unresolved Sequence cardinality, quorum, late-arrival, ontology identity/migration, journal truth, authority/admission, ARE/ACI, and exact-effect gates.

The revised package may be evidence-closed only as a repaired candidate architecture plus validation-first plan input. It must remain blocked for adoption, ontology migration, compiler/runtime conformance, promotion, deployment, release, or production.
