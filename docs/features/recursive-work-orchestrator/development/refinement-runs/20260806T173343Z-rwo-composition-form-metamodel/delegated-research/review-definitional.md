# Review Return — Definitional Soundness And System Boundary

- Agent: Meadows, Donella H.
- Action: `spawn-0007`
- Target verdict: `BLOCK — REPAIRABLE`
- Action execution: completed read-only
- Claim ceiling: candidate-local definitional review only.

The closed-envelope direction is viable, but the synthesized ontology delta collapses authored syntax, compiled graph structure, runtime identity, and external decision ownership. It must not pass unchanged.

## Candidate verdicts

| Candidate | Verdict | Required repair |
|---|---|---|
| `FormDefinition` | REPAIR | Make it an immutable authored composition-source artifact under existing `CompositionForm`, distinct from `CompositeWorkDefinition`; remove duplicated version identity. |
| `FormInstance` | KILL | Current shape duplicates `WorkNode` and falsely suggests a run. Replace, if necessary, with compile-time `FormOccurrence`; runtime remains `WorkRun`/`Attempt`. |
| `OperandBinding` | REPAIR | Kill the generic target union; use closed per-form role/domain. External owner references are not operands. |
| `ConditionBinding` | KILL | Generic condition/evaluator/phase is a universal-policy extension seam. |
| `EventSelectionCondition` | KILL | Re-skinned `EventSelector`. |
| `StructuralReadinessCondition` | KILL | Re-skinned `ReleasePolicy`. |
| `RouteMatchCondition` | KILL | Exact opaque-label equality is edge routing behavior, not a node. |
| `BoundAvailabilityCondition` | KILL | RWO consumes accepted debit evidence; journal/budget owner decides/records capacity. |
| `LateArrivalPolicy` | BLOCK/KILL pending owner | Retain only immutable join plus evidence-retention invariant until a distinct closed behavior/owner exists. |
| `CompilerProfile` | REPAIR | Immutable canonicalization/desugaring configuration only; not plugin/evaluator/policy/proof/authority. |
| `CompilationReceipt` | REPAIR | Use disjoint success and rejection results; success targets `WorkGraph`; separate raw-source and canonical-input digests. |
| unified defect vocabulary | KILL | Retain compiler-owned `FormCompilationDefect`; split runtime, journal, recovery, admission, and effect results by owner. |
| `produces-explicit-composition` | REPAIR | `ExplicitComposition` is source syntax, not output graph; result links source form definition to output `WorkGraph`. |
| generic external-reference carriage | KILL/REPAIR | Use exact typed owner-issued references; compilation checks shape only; independently accepted owner verdict gates delivery/effect. |

## Required non-collapse repairs

1. Compile-time validation covers closed syntax, references, cardinalities, and graph invariants only; runtime matching/folding is separate.
2. Release is structural eligibility, never success/approval/truth/permission; Gate compares opaque label only.
3. Sidecar cancellation direction must be explicit; companion failure/lifecycle cannot control the primary without an ordinary explicit edge and accepted authority.
4. Preserve distinct identities for transport redelivery, Work retry, domain rework round, replay/resume, effect reconciliation/retry, and recovery classification.
5. Structural graph acceptance, journal acceptance, ARE evidence, ACI admission, authority verification, and effect permission are distinct gates.
6. Separate semantic source, compiled runtime graph, generated ontology projection, validation evidence, and promotion authority.

## Tautologies and insufficient controls

“Closed” is not evidence until exact enumerations/domains/ranges/negative fixtures exist. `authority_effect: none` is necessary metadata, not proof. Compilation success/rejection shapes must be disjoint. References do not perform gating; owner verdicts do. Raw source and canonical-input digests need explicit byte domains. Compatibility must name which bytes may remain identical.

Evidence closure requires a bounded synthesis revision that kills the generic condition/evaluator layer and current `FormInstance`, repairs source/graph/result lineage, separates defects by owner, closes sidecar direction and identity distinctions, uses typed external references, and preserves unresolved owner/migration gates.
