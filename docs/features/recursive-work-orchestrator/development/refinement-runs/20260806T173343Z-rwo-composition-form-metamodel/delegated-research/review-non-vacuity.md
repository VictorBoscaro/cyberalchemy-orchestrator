# Review Return — Finite Non-Vacuity

- Agent: Capucci, Matteo
- Action: `spawn-0006`
- Target verdict: `BLOCK`
- Action execution: completed read-only
- Claim ceiling: candidate research only; no fixture execution, schema/compiler/runtime conformance, or ontology adoption.

The synthesis’s acceptance section is a checklist of future witnesses, not a witness package. It has no fully serialized canonical inputs, exact output graph/receipt bytes and digests, event histories, before/after state snapshots, executable fixtures, or validation receipts. Several negatives permit multiple or unnamed defects and are therefore not discriminating.

## Candidate verdicts

| Candidate | Verdict | Reason |
|---|---|---|
| Common envelope plus seven payloads | KILL-AS-SUBMITTED | No concrete nested all-forms input/expected graph. |
| Closed condition taxonomy | KILL-AS-SUBMITTED | Prose families, not closed schemas; external-owner negatives are not distinct. |
| Deterministic compiler/receipt | KILL-AS-SUBMITTED | No canonical bytes, output bytes, digest values, or second receipt. |
| FanIn concurrency/late arrival | KILL-AS-SUBMITTED | Owner gates open; no finite schedule proves freezing. |
| Gate ambiguity behavior | KILL-AS-SUBMITTED | Duplicate labels are compile-invalid, so runtime ambiguity needs an explicit corruption/migration path. |
| Repeat/retry separation | KILL-AS-SUBMITTED | One negative code collapses five distinct owner/identity transitions. |
| Version/compatibility | KILL-AS-SUBMITTED | No schemas/fixtures and current ontology identity is divergent. |
| Unified defects/scenario matrix | KILL-AS-SUBMITTED | `or`, matching/unnamed defects, and incomplete mutation boundaries. |
| Ontology delta | BLOCK | No query fixtures or owner acceptance; identity/migration unresolved. |
| Accepted external references | KILL-AS-SUBMITTED | Exact reference shapes and owners unresolved. |
| Generic expression/policy evaluator | KILL-CONFIRMED | Owner collapse is inherent. |

## Witness repairs

- Supply one serialized nested all-seven-form tree, exact expanded node/edge set, maximum depth, and expected receipt.
- Compile identical canonical bytes twice and bind byte sizes plus graph/receipt digests.
- Split invalid Sequence selector at definition time from ordinary runtime no-match.
- Provide separate FanOut empty and duplicate fixtures.
- Close quorum owner, then test `0`, above-count, and positive boundary schedules.
- Close late-arrival owner, then prove evidence append with frozen manifest/command/quorum set.
- Keep `ROUTE_LABEL_UNMATCHED`; use `GATE_ROUTE_DUPLICATE` at compile time; retain runtime ambiguity only for explicit corruption/migration.
- Show Sidecar evidence retention without primary mutation.
- Split Repeat bound missing, non-positive, exhaustion missing, and runtime exhausted cases.
- Separate redelivery, Work retry, domain rework round, recovery decision, replay, and effect reconciliation.
- Provide bounded/unbounded explicit-cycle pairs, distinct forbidden-owner predicate defects, split stale-version defects, split digest defects, and exact owner/admission missing-reference schemas.

## Mutation boundaries

Compile rejection emits no graph, success receipt, runtime state, journal record, cursor, or command. Runtime negatives may retain accepted evidence but must not mutate the forbidden control surface: no edge/command on a nonmatch, no frozen join rewrite, no route on unmatched Gate, no primary change from Sidecar failure, no new debit/round/attempt after exhaustion, no current release from stale attempt, no cursor rewrite from stale reducer, quarantine only for divergent bytes, and no inferred/minted owner reference.

Evidence closure therefore cannot claim finite non-vacuity. It may retain the source-backed seed as an authored candidate and produce a validation-first plan whose first units materialize exact schemas, fixtures, compiler/validator, digests, and receipts.
