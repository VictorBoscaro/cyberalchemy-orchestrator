# W2 — Failure, sealing and observation

- **Layer:** L1 / S-002.
- **Entry:** W1 pass and Slice-1 ADRs accepted.
- **Order:** SWU-ACI-012 -> SWU-ACI-013/014/015/016 -> combined fault/security matrix.
- **Parallelism:** ACL, race and projection implementations may run in disjoint modules after shared
  event/capability schemas freeze.

## Exit gate

All sealing surfaces deny pre-reveal peer content; crash recovery converges; every race has one
allowed terminal; expired SSE cursors recover through a consistent snapshot; sandbox/credential
setup fails closed.

