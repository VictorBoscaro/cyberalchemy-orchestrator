# W0 — Decision and contract baseline

- **Layer:** L0 / S-000.
- **Entry:** planning artifacts exist.
- **Completed:** TASK-000 / SWU-ACI-001.
- **Completed:** SWU-ACI-002 and the W0 closure corpus passed independent review cycle 5/5.
- **Selected next mutation:** exact `SWU-ACI-APT-VS-001` only; its descriptor-bound mutation scope
  passed the cross-workpack owner/reviewer gate.
- **CVR adjunct lane:** TASK-CVR / `SWU-ACI-CVR-000` is documentation-prepared only.
  `SWU-ACI-CVR-GUARD-001`, `001` and `002` remain blocked by both global gates and absent nominal
  authorizations; this lane does not complete TASK-000 or W0.

## Exit gate

1. Persistence, offset, idempotency, terminal, snapshot, decision and ledger-consistency ADRs are accepted.
2. Golden command/event/state trace and opening/close row fixtures are frozen.
3. EG-1 drift has a disposition and sole-writer guard specification.
4. Legacy/runtime ownership and rollback are explicit.
5. `WORK-PACK.md` and this execution pack are deliberately promoted from `block` to `pass` for L0.

No runtime code starts in W0.

## CVR ApprovalPacket checkpoint

1. Canonical IDs and Stage-1 shorthand aliases are traceable in the shared manifest.
2. Architecture, product/protocol, host/operator and root acceptances are absent until recorded
   against exact digests.
3. `cvrImplementationGateStatus=approval_packet_prepared` is non-pass and only prepared for named
   owner review; there is no active
   exception.
4. The proposed per-SWU predicate is non-operative until a five-entry packet includes the selected
   deterministic descriptor and is owner/root accepted.
5. Strict order is `000 -> GUARD-001 -> 001 -> 002`; GUARD bootstrap uses an external trusted
   executor and exact root-owned one-time authorization, never itself.
6. Each execution has exactly three content-addressed authority artifacts. GUARD bootstrap has
   exactly one external authority-owned bootstrap finalizer; CVR-001/002 use only the common
   guard/finalizer. Root requests cancellation/BLOCK and never writes a receipt. CVR-002 binds the CVR-001 PASS receipt/baseline, allowed delta and
   pre-write hashes and reruns all CVR-001 tests.
7. AuthoritySlot principals/acceptances and future serialized authority artifacts are absent;
   the session root orchestrator is final approver only, not a substitute for named owners.
8. No execution claim exists; root-owned create-exclusive claim creation is post-authorization,
   precedes writer invocation, and is the sole initial governance-write exception.
9. The claim is the only immutable lease and the launch context is ephemeral and external, so
   exactly three authority artifacts remain persisted. Trust policy/provider, repository binding,
   authenticated principals, trusted UTC/nonces and target-filesystem CAS evidence are absent and
   keep bootstrap blocked; workspace hashes prove integrity only.
