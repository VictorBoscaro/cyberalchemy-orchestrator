# Invoke Define context
## Goal

Turn accepted Phase-A decisions D1/D2 into ACI normative contracts before implementation planning.

## Approved intent

- Exact host-observed terminal response bytes are persisted as content-addressed evidence.
- Only a verified receipt for those bytes may satisfy bounded `binding-output`.
- Python + SQLite remains the implementation baseline.
- Agents do not copy or re-author upstream outputs.
- Connections remain topology; a pre-confirmed slot mapping owns data materialization.

No interview question remained open: the owner decisions, feature owner and compatibility boundary
were already explicit. Existing workflow-graph discovery satisfies the discovery-existence gate.
