# Template selection
Selected: existing ACI DomainSpec family (`SPEC.md` plus domain, operations, states, events,
interfaces and glossary aspects).

Eligibility: the target is an amendment to an established DomainSpec feature, not a new feature or
candidate template family. The repository-local inventory is valid but has no entries; the existing
ACI aspect structure and canonical Invoke DomainSpec template therefore provide the only eligible
non-duplicative shape. There was no tie requiring another owner choice.
