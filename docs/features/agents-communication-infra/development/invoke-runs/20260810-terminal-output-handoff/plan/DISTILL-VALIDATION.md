# Distill validation

## Intent and budget

- Target: implementation plan for exact terminal-output handoff through the research topology.
- Artifact: split work-pack with L0–L3 and executable SWUs.
- Mode: Standard, two recursive rounds; user did not select another budget.

## Round 1

Proposer: a staged compiler over existing receipts can solve the lifecycle. Balancers objected that declared files contradict D1, exact final text was not captured, parent close ignores never-launched seats, and idempotent bindings do not prevent duplicate spawn intent.

Reconciliation:

- rejected declared-file attribution;
- selected a host-owned `list_agents` result as an L0 hypothesis, not a claim;
- split byte commit (HTR-001) from host capture (HTR-002);
- deferred 1→1, fan-in and live pilot to L1–L3;
- added durable stage/no-relaunch and declared-seat close obligations.

## Round 2

An independent Balancer returned `block` with seven findings. Reconciliation:

- accepted: add HTR-000 to prove the real hook payload before runtime mutation;
- accepted: limit the executable frontier to L0/L1; L2/L3 require normative revalidation;
- accepted: split fan-in (HTR-004A) from declared-seat close (HTR-004B);
- accepted: split research (HTR-005) from independent review (HTR-006);
- accepted: remap TOH-002/004/005 to the boundaries that can actually prove them;
- accepted: research owns `research.md` and `findings.md`; review alone owns `review.md`;
- accepted: use repo-relative execution-entry paths and include Task Session baseline/source receipts in mutation scopes;
- already repaired before closeout: all Markdown links resolve from their actual files.

## Current checks

| Check | Result | Evidence/gap |
| --- | --- | --- |
| Smallest coherent unit | pass after repair | HTR-000 is evidence-only; HTR-001 remains a direct service primitive. |
| SWU atomicity | pass after repair | HTR-004 and HTR-005 were split at independently accepted boundaries. |
| First-unit narrowness | pass after repair | HTR-000 can stop the route without runtime source mutation. |
| Recomposition | pass first round | Traceability maps units to TOH-001–008 and research pilot. |
| Hidden critical gaps | block until HTR-000 | Host payload exactness is explicit and falsifiable before HTR-001. |
| Deferred complexity | pass | General graphs, cycles, degraded success and provider-complete inputs remain excluded. |
| Navigation | pass | WORK-PACK → W0 → TASK-HTR-CAPTURE → HTR-001. |

## Premortem

Most likely failure: the Codex list result is normalized, truncated or cannot be correlated uniquely. Guardrail: fail W0, leave downstream compiler fence active, and reopen host integration design; never fall back to a repository path.

## Frame expiry

Re-run design/readiness if Codex changes the collaboration tool response contract, if a direct terminal-response hook becomes available, or if the required topology exceeds bounded acyclic complete fan-in.

## Verdict

`block` for mutation-capable Task Session until HTR-000 produces a passing real-host receipt. The repaired plan is navigable and structurally valid; on a passing preflight, refresh Distill to `pass`, reclassify the entry to `selection-ready`, and select HTR-001. On failure, keep the compiler fence and reopen host integration design.
