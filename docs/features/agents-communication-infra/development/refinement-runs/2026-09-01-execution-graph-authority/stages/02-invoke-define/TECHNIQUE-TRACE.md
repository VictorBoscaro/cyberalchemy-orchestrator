# Invoke Define technique trace

- `sequence`: intent → compiled graph → presented projection → observation → acceptance → runtime.
- `frame_handoff`: the strict context pack supplies fixed decisions, evidence and exclusions.
- `minimum_component_catalog`: eight authority components cover identity, work, topology,
  capabilities, policies, lifecycle, canonicalization and presentation.
- Fallback residue: the installed Invoke package has no `define.md`; the local DomainSpec
  specification template was used as the closest native definition contract.
