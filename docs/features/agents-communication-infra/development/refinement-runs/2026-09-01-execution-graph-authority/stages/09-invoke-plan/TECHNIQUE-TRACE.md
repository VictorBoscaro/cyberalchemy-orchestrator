# Invoke Plan technique trace

- `implementation-layering`: L0 specification, L1 conformance, L2 compiler/preview, L3 acceptance,
  L4 execution.
- `work-pack`: one bounded first unit with inputs, outputs, criteria, negatives and stops.
- `scu_swu_reduction`: the coherent design is larger than the first authorized work unit.
- `recomposition_proof`: every layer has an evidence gate before the next.
- `execution_receipt_handoff`: the work pack requires an independent spec receipt before code.
- Fallback residue: the installed Invoke package has no `plan.md`; local
  `implementation-layering.md` and `work-pack.md` templates were used.
