# Invoke Design technique trace

- `artifact_contract_bridge`: graph, projection, observation, accepted envelope and runtime state
  have explicit owners and digest relations.
- `owner_boundary_check`: one JSON remains the only logical authority; no second plan artifact was
  introduced.
- `sequence`: compilation, presentation, observation, acceptance and execution are ordered.
- Six architecture views cover system context, components, authority, presentation, runtime
  sequence and compatibility/deployment.
- Fallback residue: the installed Invoke package has no `design.md`; the local
  `domainspec-spec/architecture-bundle.md` template supplied the design structure.
