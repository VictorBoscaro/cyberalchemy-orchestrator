# Task session — SPEC-ACI-AGENT-IDENTITY-ROLE-001

- Route: bounded specification repair after an independent `FIX` review and two `FIX` rechecks.
- Objective: repair AIR-R9..R10 without production-code mutation or alteration of `review.md`.
- Evidence scope expanded only to Recheck 2, installer/package selectors, route-strategy copies,
  confirmation producer/test, bounded literal searches, migration surface inventory and the two
  conflicting feature-scoped Craft decision sentences.
- Owner choice applied: final field remains `display_name`; canonical YAML key is `agent_name`;
  registry v1 is the closed eight-role set with singular `other` and future versioning.
- Real evidence: pool is exactly two documents, v0.6.0, 414 unique legacy `name` rows, raw digest
  `5c7b...eba`, with `field/era/role_fit/cited/tags` and optional `note` preserved.
- Write scope used: this follow-up except immutable `review.md`, plus feature `CRAFT.md` and feature
  `.craft/ledger.yml` evidence wording. Production pool/code/telemetry and repository-root ledger
  were read only; the feature artifact remains `candidate`, with no authority promotion.
- Validation: existing schema/fixture harness, bounded migration-surface classification and Craft
  consistency only; production consumers and telemetry row versions remain stale.
- Subagent lifecycle: this is the worker seat; the independent reviewer must recheck. No self-approval.
