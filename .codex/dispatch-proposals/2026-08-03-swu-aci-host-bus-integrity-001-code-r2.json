{
  "dispatch_id": "2026-08-03-swu-aci-host-bus-integrity-001-code-r2",
  "schema_version": "0.6.3",
  "dispatch_type": "code",
  "goal": "Close Phase-A findings F4, F5 and F6 for SWU-ACI-HOST-BUS-INTEGRITY-001 without changing the accepted Host Binding or BUS behavior: a follow-up must require a non-empty persisted prior agent_id and exact target equality before any new binding row or event can commit (F4); materialization must load every official peer artifact body and recompute its digest before trusting stored contribution or reveal hashes (F5); and the Stage-E source manifest must bind reveal_delivery.py and migration 011 with tamper tests failing closed for both active sources (F6).",
  "context": "SECOND ATTEMPT. The first dispatch (2026-08-03-swu-aci-host-bus-integrity-001-code) closed user_abort at the alignment gate: both auditors independently returned NOT PASS because implementations/server/runtime/local_pilot.py was missing from write_scope, and the coder was therefore never launched. The descriptor and readiness receipt have now been revised to include it, narrowed to the STAGE_E_SOURCE_MANIFEST_SHA256 constant. AUTHORITY CAVEAT, recorded deliberately: nothing in this repository or in the linked DomainSpec package produces a domainspec-code-readiness@1 receipt — the schema has a strict validator, two consumers and two instances, but no producer. The revised descriptor and receipt were authored by the same agent that authored the code contract they are checked against, so the appender's readiness check is SELF-CONSISTENCY, not independent planner evidence. The owner accepted this explicitly rather than treating it as satisfied planner authority. BASELINE: HEAD is d1716bd. The six suites were verified green at 97c51f5 (runtime 119, control_center 36, test_ledger, test_main, test_ui 10/10, agent-runtime 31) and the three commits since then touch only docs, planner artifacts and dispatch records, never runtime source; Stage-E source integrity re-verified PASS across 47 pinned sources at d1716bd. The tree is NOT quiescent: a separate research dispatch, 2026-08-03-cyberalchemy-orchestrator-capability-assessment-r2, is registered and still open in this same append-only ledger from another session, writing into the sibling repo ../subagent-work-infrastructure. The owner authorized proceeding anyway. If the appender or bridge rejects on ledger drift, that is the expected failure mode and must be recorded, not worked around. CONTAINMENT NOTICE for every seat: the declared capability_profile of filesystem task-scoped-write is NOT technically enforced, because tool_profile_ref is host/inherited@1 and the host surface is used as-is. A seat of the previous attempt at this SWU wrote to files outside its remit and outside write_scope. The scope boundary in your prompt is the only boundary that exists; treat it as binding. KNOWN INFRASTRUCTURE LIMITATION: compile_bound_launch_plan binds every seat at turn_ordinal 0 with slots [] and never reads the confirmed connections graph, so no authorized input is delivered automatically between waves. The parent relays each wave's frozen output; that relay is a named workflow limitation, not Work Bus evidence. Do not synthesize a follow-up envelope by hand — the host hook rejects unbound followup_task.",
  "max_loops": 2,
  "final_approver": "victorboscaro@gmail.com",
  "anti_bias_mode": "disabled",
  "working_folder": "docs/features/agents-communication-infra/work-pack/execution/",
  "code_contract": {
    "type_skill_ref": ".claude/skills/domainspec-implement/SKILL.md",
    "type_skill_digest": "sha256:dee8cfbfa23a2acc3dbd81da18cef87f061301b668716c595bdefe4e81d3aacf",
    "work_pack_ref": "docs/features/agents-communication-infra/work-pack/descriptors/SWU-ACI-HOST-BUS-INTEGRITY-001.json",
    "work_pack_digest": "sha256:52220de40eac7cab054ec11a05578ca1972643c547c41616429396789687f50f",
    "test_spec_ref": "docs/features/agents-communication-infra/TEST-SPEC.md",
    "test_spec_digest": "sha256:84182cc5a6118edf94ca1e99d371abfb795fba3914f91f2d7fe4fbefd34f9b00",
    "readiness_ref": "docs/features/agents-communication-infra/work-pack/execution/SWU-ACI-HOST-BUS-INTEGRITY-001-code-readiness.json",
    "readiness_digest": "sha256:c5b863b0f655d00682c60525e27036d8adabce469eb8f5997a6b24ee439f30a0",
    "brownfield": true,
    "write_scope": [
      "implementations/server/runtime/service.py",
      "implementations/tests/runtime/test_host_workflow_binding.py",
      "implementations/tests/runtime/test_bus_reveal_delivery.py",
      "docs/features/agent-provenance-telemetry/integration/stage-e/source-manifest.json",
      "implementations/tests/runtime/test_orchestration_bridge.py",
      "implementations/tests/runtime/aci-test-traceability.json",
      "implementations/tests/runtime/test_aci_traceability.py",
      "implementations/server/runtime/local_pilot.py"
    ],
    "validation_commands": [
      "python -B -m unittest implementations.tests.runtime.test_host_workflow_binding -v",
      "python -B -m unittest implementations.tests.runtime.test_bus_reveal_delivery -v",
      "python -B -m unittest implementations.tests.runtime.test_orchestration_bridge -v",
      "python -B -m unittest implementations.tests.runtime.test_aci_traceability -v"
    ],
    "alignment_group_id": "alignment-audits",
    "implementation_group_id": "implementation",
    "verification_group_id": "verification"
  },
  "groups": [
    {
      "group_id": "alignment-audits",
      "n": 2,
      "agents": [
        {
          "agent_name": "Jackson, Michael",
          "role": "auditor",
          "model": "claude-sonnet-5",
          "token_budget": 1200,
          "initial_prompt": "Alignment RE-AUDIT for SWU-ACI-HOST-BUS-INTEGRITY-001, second attempt. A prior alignment audit already established the following and you should VERIFY rather than rediscover it: F4, F5 and F6 are each determinate against the accepted Host Binding and BUS contracts, with no explicit-non-goal contamination; F4 = drop the 'is not None' guard at service.py:5529-5535 so a null persisted agent_id itself refuses the follow-up while exact target equality is kept; F5 = official_rows at service.py:3083-3092 selects only a.content_hash and never a.body, and build_peer_entries (reveal_delivery.py:52-90) only compares stored hashes, so the fix is to select a.body and rehash, mirroring the pattern already present for base_rows at service.py:3093-3102; F6 = the Stage-E manifest currently omits reveal_delivery.py and migrations/011_bus_reveal_delivery.sql, both live sources (imported at service.py:46-53, registered at database.py:24-25). The PRIOR BLOCKER was that implementations/server/runtime/local_pilot.py was absent from write_scope while its hardcoded STAGE_E_SOURCE_MANIFEST_SHA256 must change in lockstep with any manifest edit. It has now been ADDED to write_scope, narrowed to that constant. Your job: read the revised descriptor at docs/features/agents-communication-infra/work-pack/descriptors/SWU-ACI-HOST-BUS-INTEGRITY-001.json and the revised receipt at work-pack/execution/SWU-ACI-HOST-BUS-INTEGRITY-001-code-readiness.json, then answer exactly: (1) does the revised write_scope now cover every file that must change to close F4-F6 and keep the four validation commands passing, or is anything still missing; (2) does adding local_pilot.py create any new path to an explicit non-goal (F1, F2, F3, effect claim, provider or tool execution, ACI-005 materializer, runtime cutover, external network, production), given it is narrowed to one constant; (3) is the secondary finding about test_aci_traceability.py's spec-ID regex correctly in scope; (4) does the descriptor's stated editing constraint (surgical digest-token substitution, never json.dumps re-serialization, constant and manifest in the same commit) correctly describe what F6 requires. Return PASS only if the revised scope is sufficient, non-goal-clean, and the editing constraint is correct. Return a concrete blocking list otherwise. CONTAINMENT, binding: you write NO files anywhere in this repository or any other — not code, not documentation, not notes, not receipts, not a scratch file. Your entire deliverable is the report you return in your final message. If you conclude a file must change, say so in the report and do not change it. This prohibition is enforced ONLY by this instruction: the runtime grants you the full host filesystem surface and the declared capability_profile binds nothing technically. An auditor seat of the previous attempt at this same SWU wrote to a different feature's research artifacts outside its remit; do not repeat that. You do not write code and you do not approve your own or the coder's work."
        },
        {
          "agent_name": "Parnas, David",
          "role": "auditor",
          "model": "claude-sonnet-5",
          "token_budget": 1200,
          "initial_prompt": "Layering RE-AUDIT for SWU-ACI-HOST-BUS-INTEGRITY-001, second attempt. A prior layering audit already established the following and you should VERIFY rather than rediscover it: service.py exclusively owns the host_workflow_turn_bindings lifecycle (migration 009), so F4 belongs there; service.py's materialize_authorized_peer_input already does raw-SQL load plus digest_bytes compare for base/role-delta refs at service.py:3093-3102 but not for official peer rows, so F5 extends an existing local pattern with no new import; local_pilot.py IS the correct owner of Stage-E manifest verification, and moving that check into service.py would create the cycle service -> local_pilot -> service since local_pilot imports service and never the reverse, so F6 must stay data-only in the manifest with no new service.py logic. The PRIOR BLOCKER was that local_pilot.py was absent from write_scope; it has now been ADDED, narrowed to the STAGE_E_SOURCE_MANIFEST_SHA256 constant at local_pilot.py:37-39. Your job: read the revised descriptor and receipt, then answer exactly: (1) does editing ONLY that constant in local_pilot.py preserve its ownership boundary, or does it smuggle verification responsibility across a module edge; (2) with local_pilot.py now writable, is there any remaining temptation or requirement to duplicate _verify_file or _verify_source_manifest logic elsewhere, and is that forbidden; (3) does the pre-existing duplication of the load-and-rehash primitive between artifacts.ArtifactStore.get_authorized and service.py's inline raw-SQL path get worse under F5, and should that be recorded as a follow-up rather than fixed here; (4) name any file still outside write_scope that a layering-clean implementation of F4-F6 would have to touch. Return PASS only if F4-F6 can now be implemented inside the revised write_scope without inverting a dependency or duplicating an owner's responsibility. Return a concrete blocking list otherwise. CONTAINMENT, binding: you write NO files anywhere in this repository or any other — not code, not documentation, not notes, not receipts, not a scratch file. Your entire deliverable is the report you return in your final message. If you conclude a file must change, say so in the report and do not change it. This prohibition is enforced ONLY by this instruction: the runtime grants you the full host filesystem surface and the declared capability_profile binds nothing technically. An auditor seat of the previous attempt at this same SWU wrote to a different feature's research artifacts outside its remit; do not repeat that. You do not write code and you do not approve your own or the coder's work."
        }
      ]
    },
    {
      "group_id": "implementation",
      "n": 1,
      "agents": [
        {
          "agent_name": "Ousterhout, John",
          "role": "coder",
          "model": "claude-opus-5",
          "token_budget": 5000,
          "initial_prompt": "Implement SWU-ACI-HOST-BUS-INTEGRITY-001 (F4, F5, F6) only. You may start only after BOTH alignment auditors have returned PASS; the parent will relay their verdicts verbatim because the runtime does not deliver inter-wave input automatically. Write ONLY inside the revised write_scope, which is now eight paths: implementations/server/runtime/service.py, implementations/server/runtime/local_pilot.py (RESTRICTED to the STAGE_E_SOURCE_MANIFEST_SHA256 constant at lines 37-39 and nothing else in that module), implementations/tests/runtime/test_host_workflow_binding.py, implementations/tests/runtime/test_bus_reveal_delivery.py, implementations/tests/runtime/test_orchestration_bridge.py, implementations/tests/runtime/aci-test-traceability.json, implementations/tests/runtime/test_aci_traceability.py, and docs/features/agent-provenance-telemetry/integration/stage-e/source-manifest.json. TWO INDEPENDENT AUDITS have already located the work; implement exactly this and stop if reality diverges. F4: at service.py:5529-5535 the current gate reads 'if prior_turn[\"agent_id\"] is not None and target != prior_turn[\"agent_id\"]: raise AuthorizationError'. Drop the 'is not None' guard so a null or empty persisted agent_id itself refuses any follow-up, while a non-empty identity still requires exact target equality. Placement before the workflow-manifest read at line 5536 is already correct; do not move it. F5: official_rows at service.py:3083-3092 selects only 'm.*, a.content_hash AS artifact_content_hash' and never a.body, and reveal_delivery.build_peer_entries (reveal_delivery.py:52-90) only compares stored hash fields. Add a.body to that select and rehash digest_bytes(bytes(row[\"body\"])) against artifact_content_hash and the reveal payload_hash for every peer message before it enters build_peer_entries, raising IntegrityError on mismatch. The identical pattern already exists ten lines below for base_rows at service.py:3093-3102 — follow it, do not invent a new one, and do not import artifacts.py. F6: add exact SHA-256 entries for implementations/server/runtime/reveal_delivery.py and implementations/server/runtime/migrations/011_bus_reveal_delivery.sql to the Stage-E manifest's files map. CRITICAL EDITING CONSTRAINT: edit the manifest by surgical substitution of digest tokens in the raw text; NEVER json.dumps re-serialization, because the file is CRLF on disk and a round-trip is not byte-identical. Then recompute the manifest's own SHA-256 and update STAGE_E_SOURCE_MANIFEST_SHA256 in local_pilot.py:37-39, plus service.py's own pinned entry in the manifest after your F4/F5 edits. All of that must be internally consistent in one pass: _verify_source_manifest checks the manifest's own bytes before any per-file entry, and orchestration_bridge.run() calls preflight_local_pilot on every CLI open/close. Add source-linked tests for T-ACI-PHASEA-I1 (null-identity and mismatched-identity follow-ups both raise and leave zero new rows in host_workflow_turn_bindings, events, receipts and artifacts), I2 (corrupted peer body bytes with hashes left untouched raise IntegrityError with zero writes, and the unchanged fixture still materializes and retries byte-identically), and I3 (mutating either newly bound file's bytes in an isolated copy makes _verify_source_manifest raise GateBlockedError before the bridge/BUS path is reachable). Each test must FAIL if its guard is removed. Also widen test_aci_traceability.py's spec-ID regex, currently T-(?:ACI|CVR)-[A-Z0-9]+, so it matches the hyphenated T-ACI-PHASEA-I1/I2/I3 headings; it currently returns zero matches. Explicit non-goals you must NOT touch: F1 invocation-plan authority, F2 producer-output evidence including complete_host_workflow_turn's agent_id=None acceptance, F3 completion receipt and status restoration, effect claim, provider or tool execution, the ACI-005 opening materializer, runtime cutover, external network, production. Run all four validation commands plus the full runtime suite (python -m unittest discover -s implementations/tests/runtime -t .), which was green at 119 tests before you started, and verify Stage-E source integrity still passes. Report exact commands and exit results, inspect your actual diff before reporting, and never claim completion from prose. CONTAINMENT, binding: the eight write_scope paths are the ONLY files you may create or modify, anywhere. Do not write scratch files, notes, receipts, reports, or documentation outside them, and do not touch any other repository. If you conclude a file outside write_scope must change, STOP and report it as a blocking scope conflict rather than editing it — that is exactly how the previous attempt at this SWU was correctly caught. This prohibition is enforced ONLY by this instruction: the runtime grants you the full host filesystem surface and the declared capability_profile of filesystem task-scoped-write binds nothing technically. Run git status yourself before you report and include its output, so any escape is visible in your own evidence."
        }
      ]
    },
    {
      "group_id": "verification",
      "n": 1,
      "agents": [
        {
          "agent_name": "Randell, Brian",
          "role": "skeptic",
          "model": "claude-opus-5",
          "token_budget": 3000,
          "initial_prompt": "Verify the SWU-ACI-HOST-BUS-INTEGRITY-001 implementation. The parent will relay the coder's task-owned diff, source-linked test results and the pinned input contract to you, because the runtime does not deliver inter-wave input automatically. You never accept prose as evidence, and you were not involved in locating the work, so treat the coder's account of what the audits said as a claim to check. Check exactly: (1) does the diff stay entirely inside the eight declared write_scope paths, with no path escape, and is the local_pilot.py edit confined to the STAGE_E_SOURCE_MANIFEST_SHA256 constant with no other change in that module; (2) does each of F4, F5, F6 have a test that FAILS when the guard is removed, not merely a test that passes with it present — name the test and the exact mutation for each, and actually run at least one such mutation; (3) for F4, is a null, empty or mismatched prior agent_id refused before any binding row or event commits, and is target equality exact rather than partial or prefix; (4) for F5, is EVERY official peer artifact body reloaded and rehashed, or does any code path still trust a stored hash, and was the check placed before build_peer_entries rather than inside it; (5) for F6, are reveal_delivery.py and migration 011 both bound in the manifest AND do tamper tests fail closed for both, in isolated copies rather than by mutating the real tree; (6) is the manifest self-digest consistent — recompute the manifest's SHA-256 yourself and compare it to STAGE_E_SOURCE_MANIFEST_SHA256, and confirm service.py's own pinned entry matches its post-fix bytes; (7) did the full runtime suite stay green at or above 119 tests, and do the four declared validation commands pass with the exact commands recorded; (8) did any explicit non-goal get touched, especially complete_host_workflow_turn's agent_id=None acceptance (F2 territory). Any failing check returns to the coder within the loop ceiling of 2. Report residual risks explicitly, including what F4-F6 does NOT protect and any pre-existing duplication the change perpetuates. CONTAINMENT, binding: your mutation testing in check (2) is the ONE exception to a general no-write rule, and it is bounded — perform each mutation, observe the failure, and restore the exact original bytes immediately, or work in a copy outside the repository. Run git status before you report and include its output to prove you left no residue. Write no other file anywhere: no notes, no scratch files, no report artifacts, no documentation. This is enforced ONLY by this instruction; the runtime grants you the full host filesystem surface. You do not approve the dispatch; the final approver is outside every working group."
        }
      ]
    }
  ],
  "connections": [
    { "from": "alignment-audits", "to": "implementation", "type": "sequential" },
    { "from": "implementation", "to": "verification", "type": "sequential" }
  ]
}
