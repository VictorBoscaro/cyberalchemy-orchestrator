# Mint — Experiment Harness Run Report

- RUN_ID: mint-smoke-01-20260701T180723Z
- TIMESTAMP: 2026-07-01T18:07:23Z
- MODE: run (smoke regime)
- ARTIFACT: .claude/skills/mint/SKILL.md
- PROFILE_ID: sigil-development
- LIFECYCLE_OWNER: sigil-development
- ARTIFACT_TYPE: sigil
- CONTRACT_PATH: .claude/skills/mint/SKILL.md
- PROMPT_SET: development/example-prompts/
- REGIME_SET: development/regimes/ (smoke; loop-first not yet populated)
- PROFILE_VALIDATION: pass
- SELECTION: mint-smoke-01 (fresh domain `tide-tables` — generalization probe beyond the resonantos fixture)
- OUTPUT: scratch `.../mint-smoke-01/tide-tables/` (10 files; test evidence, not a repo mint)
- VALIDATION: **pass** (independently re-verified by the parent, not just runner-reported)
- QUALITY_BAR_STATUS: pass
- ANTI_PATTERN_HITS_JSON: []
- WORKFLOW_GAPS_JSON: [{"category":"emit-logic-coverage","severity":"low","summary":"§7 had no tree-wide zero-placeholder check; the import-pack slot template could emit with dangling <...>","evidence":"self-check #1 covers AUTHORITY-MODEL only","status":"FIXED — added §7 check #10 + specimen carve-out"}]
- OBSERVATION: recorded (.arcanum/observability/signals/sigil-invocations.jsonl)

## What ran
A native subagent played Mint: read `EMIT-PROCEDURE.md` + the composed templates + cav2's AUTHORITY-MODEL
(D49 kinds, 8 postures) and constitutions (CAV2-C*), then emitted the `tide-tables` authority spine
(core + 13 additions, Steps 1–3 + 7–12) into a scratch target and ran §7 self-checks 1–9.

## Self-check results (independently re-verified)
| # | check | verdict | evidence |
|---|---|---|---|
| 1 | parses / no dangling `<...>` | pass | 0 angle-bracket placeholders tree-wide |
| 2 | default-deny | pass | domain kind row carries a non-empty Blocked-use cell |
| 3 | no-leak (Obs-B) | pass | `grep -rniE "as (an? )?(external )?method authority"` → 0 |
| 4 | domain-originated (F3c) | pass | "prediction-provenance authority" / route "constituent-version-governance" — lexically + owner-route new vs all 9 cav2 D49 rows |
| 6 | methods present (#13) | pass | METHODS-CATALOGUE has the kind's intake/route/decide/record/demote block |
| 7 | ratify-close (#11) | pass | `grep PROPOSED` on ratified AUTHORITY-MODEL → 0; ratify record verdict = `ratify`→active |
| 8 | seed-format zero-cav2-rows (#3/#17) | pass | cav2's 8 posture names in AUTHORITY-POSTURES → 0; `CAV2-C` in CONSTITUTION-IMPORT-PACK → 0 |
| 9 | drift-audit resolves (#8) | pass | DEFINITION-DRIFT-AUDIT.md + DEFINITIONS-INDEX.md exist; C(meta-4) links both |

## Finding (FLAG → fixed)
The smoke test surfaced a genuine emit-logic coverage gap: §7 check #1 scans only AUTHORITY-MODEL, so the
intentionally-scaffolded `CONSTITUTION-IMPORT-PACK` rule-slot template could emit with raw `<n>`/`<Rule
name>` placeholders and no check would catch it. **Applied fix (loop-first, reversible):**
- `EMIT-PROCEDURE.md` §7 — added **check #10** (tree-wide zero dangling placeholders, carve-out for
  fenced fill-later specimens);
- `constitution-import-pack.md` — labeled the rule-slot format block a fill-later specimen exempt from #10.

## Verdict
**PASS.** The emit-logic (core 6 + 13 additions) executes cleanly on a domain other than the resonantos
fixture; every additions acceptance marker holds; the one gap found was fixed in the same loop. This
converts the earlier static checks into an executed pass. Claim ≤ proof: this is dev evidence on one fresh
domain + the worked example — NOT a live shipped mint, and not the frozen N=3 run.

## Next
- Optional: add `tide-tables` (or another fresh domain) as a persisted regime for the loop-first promotion path.
- Still gated: L2 signing (attestation-capability), public-attached mints (BLK-CAV2-PUBLIC-PRIVATE-001).
- After ≥5 real mints → `sigil-development --reflect`.
